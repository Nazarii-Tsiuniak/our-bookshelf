OurBookshelf 📚
Project Overview ✨
OurBookshelf — це інтерактивна онлайн-платформа для управління вашою особистою бібліотекою книг. Проєкт створений на сучасній архітектурі Angular (v20+) з використанням standalone-компонентів, що забезпечує високу продуктивність та легкість у розширенні.

Платформа надає користувачам зручний та мінімалістичний інтерфейс для каталогізації та перегляду їхньої книжкової колекції.

Основні можливості

📖 Перегляд колекції: Легко переглядайте повний список книг у вашій бібліотеці.

➕ Додавання нових книг: Інтуїтивна форма для швидкого додавання нових видань із зазначенням назви та автора.

🔍 Детальна інформація: Дізнавайтесь більше про кожну книгу, перейшовши на її персональну сторінку.

🚀 Сучасна навігація: Плавний та швидкий перехід між сторінками завдяки вбудованій маршрутизації Angular.

Concept Visualization
Тут ви можете побачити, як виглядає основний інтерфейс додатку.

[В процесі]

Технологічний стек 🛠️
Angular 20+: Проєкт побудований на останній версії популярного фреймворку для створення динамічних веб-додатків.

Standalone Components: Використовується сучасний підхід до архітектури без NgModule, що робить компоненти (book-list, book-form, book-detail) незалежними та гнучкими.

Angular Router: Для реалізації навігації між сторінками використовується RouterOutlet.

TypeScript: Мова програмування, що забезпечує надійність та масштабованість коду.

Bootstrap/CSS: Для стилізації інтерфейсу.

# OurBookshelf

This project was generated using [Angular CLI](https://github.com/angular/angular-cli) version 20.3.5.

## Development server

To start a local development server, run:

```bash
ng serve
```

Once the server is running, open your browser and navigate to `http://localhost:4200/`. The application will automatically reload whenever you modify any of the source files.

## Code scaffolding

Angular CLI includes powerful code scaffolding tools. To generate a new component, run:

```bash
ng generate component component-name
```

For a complete list of available schematics (such as `components`, `directives`, or `pipes`), run:

```bash
ng generate --help
```

## Building

To build the project run:

```bash
ng build
```

This will compile your project and store the build artifacts in the `dist/` directory. By default, the production build optimizes your application for performance and speed.

## Running unit tests

To execute unit tests with the [Karma](https://karma-runner.github.io) test runner, use the following command:

```bash
ng test
```

## Running end-to-end tests

For end-to-end (e2e) testing, run:

```bash
ng e2e
```

Angular CLI does not come with an end-to-end testing framework by default. You can choose one that suits your needs.

## Additional Resources

For more information on using the Angular CLI, including detailed command references, visit the [Angular CLI Overview and Command Reference](https://angular.dev/tools/cli) page.
